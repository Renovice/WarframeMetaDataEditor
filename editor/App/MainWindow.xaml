<Window x:Class="MetadataPatchEditor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="OpenWF Metadata Patch Editor" Height="800" Width="1240"
        Background="#1e1e1e">
    <Window.Resources>
        <Style TargetType="TextBlock"><Setter Property="Foreground" Value="#e0e0e0"/></Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#2d2d30"/>
            <Setter Property="Foreground" Value="#e8e8e8"/>
            <Setter Property="BorderBrush" Value="#3f3f46"/>
            <Setter Property="Padding" Value="3,2"/>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#0e639c"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
        <Style TargetType="CheckBox"><Setter Property="Foreground" Value="#cfcfcf"/></Style>
    </Window.Resources>

    <DockPanel Margin="10">
        <StackPanel DockPanel.Dock="Top">
            <TextBlock Text="OpenWF Metadata Patch Editor" FontWeight="Bold" FontSize="15" Margin="0,0,0,8"/>
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <Button x:Name="DecodeBtn" Content="Decode from Cache…" Padding="14,7" FontWeight="SemiBold" Click="DecodeBtn_Click"/>
                <TextBlock Text="Category:" VerticalAlignment="Center" Margin="16,0,6,0"/>
                <ComboBox x:Name="CategoryCombo" Width="240" VerticalAlignment="Center" SelectionChanged="Category_Changed"/>
                <CheckBox x:Name="GameplayCheck" Content="Gameplay only" IsChecked="True" VerticalAlignment="Center" Margin="14,0,0,0" Click="GameplayCheck_Changed"/>
                <CheckBox x:Name="BatchCheck" Content="Batch: apply to whole filtered list" VerticalAlignment="Center" Margin="18,0,0,0"/>
                <Button x:Name="DumpBtn" Content="Dump All…" Padding="12,7" Margin="18,0,0,0" Background="#3a7a3a" Click="DumpBtn_Click"/>
                <Button x:Name="MergeBtn" Content="Merge Patches…" Padding="12,7" Margin="8,0,0,0" Background="#5a4a7a" Click="MergeBtn_Click" ToolTip="Combine several patch .txt files into one (ops grouped per type, duplicates removed)."/>
            </StackPanel>
        </StackPanel>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel Grid.Column="0" Margin="0,0,10,0">
                <TextBlock DockPanel.Dock="Top" x:Name="ListLabel" Text="(decode to begin)"/>
                <TextBox DockPanel.Dock="Top" x:Name="SearchBox" Margin="0,2,0,4" TextChanged="SearchBox_TextChanged"/>
                <TextBlock DockPanel.Dock="Top" x:Name="CountText" Foreground="#888" Margin="0,0,0,4"/>
                <ListBox x:Name="ItemList" Background="#252526" Foreground="#e0e0e0" BorderBrush="#3f3f46"
                         SelectionChanged="ItemList_SelectionChanged"/>
            </DockPanel>

            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="185"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock x:Name="PathText" Grid.Row="0" Text="Decode from cache, pick a category, then an item." FontWeight="Bold" Margin="0,0,0,6" TextTrimming="CharacterEllipsis"/>

                <DockPanel Grid.Row="1" Margin="0,0,0,4">
                    <TextBlock Text="Filter fields:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="FieldFilter" TextChanged="FieldFilter_TextChanged"/>
                </DockPanel>

                <DataGrid x:Name="FieldGrid" Grid.Row="2" AutoGenerateColumns="False"
                          CanUserAddRows="False" CanUserDeleteRows="False" CanUserResizeRows="False"
                          HeadersVisibility="Column" GridLinesVisibility="Horizontal"
                          Background="#252526" Foreground="#e0e0e0" RowBackground="#252526"
                          AlternatingRowBackground="#2a2a2c" BorderBrush="#3f3f46" SelectionMode="Single">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Field" Binding="{Binding Label}" IsReadOnly="True" Width="270"/>
                        <DataGridTextColumn Header="Value" Binding="{Binding Value, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        <DataGridTextColumn Header="Op" Binding="{Binding Op}" IsReadOnly="True" Width="55"/>
                        <DataGridTextColumn Header="Location" Binding="{Binding Path}" IsReadOnly="True" Width="230"/>
                    </DataGrid.Columns>
                    <DataGrid.RowStyle>
                        <Style TargetType="DataGridRow">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsChanged}" Value="True">
                                    <Setter Property="Background" Value="#5a441c"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>

                <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="0,8,0,6">
                    <TextBlock Text="Patch file:" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <TextBox x:Name="PatchName" Width="220" VerticalAlignment="Center"/>
                    <Button x:Name="PreviewBtn" Content="Refresh Preview" Margin="8,0,0,0" Padding="10,4" Click="PreviewBtn_Click"/>
                    <Button x:Name="SaveBtn" Content="Save Patch…" Margin="8,0,0,0" Padding="10,4" Click="SaveBtn_Click"/>
                    <Button x:Name="ResetBtn" Content="Reset" Margin="8,0,0,0" Padding="10,4" Background="#5a5a5a" Click="ResetBtn_Click"/>
                    <TextBlock Text="Scale ×" VerticalAlignment="Center" Margin="18,0,4,0" ToolTip="Multiply the scaling values by this: riven strength, mod per-rank Value, or warframe per-level stats."/>
                    <TextBox x:Name="MultBox" Width="45" Text="2" VerticalAlignment="Center"/>
                    <Button x:Name="MultBtn" Content="Apply" Margin="4,0,0,0" Padding="10,4" Background="#7a5a2a" Click="MultBtn_Click"/>
                    <TextBlock x:Name="RivenReadout" VerticalAlignment="Center" Margin="12,0,0,0" Foreground="#e0c070" FontWeight="SemiBold"/>
                </StackPanel>

                <TextBox x:Name="Preview" Grid.Row="4" IsReadOnly="True" FontFamily="Consolas" FontSize="12"
                         VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                         AcceptsReturn="True" Background="#1b1b1b" Foreground="#d0d0d0"/>

                <TextBlock x:Name="Status" Grid.Row="5" Margin="0,6,0,0" Foreground="#8fce7f" TextWrapping="Wrap"/>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
